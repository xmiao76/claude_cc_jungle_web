[tool.pytest.ini_options]
testpaths = ["tests/python"]
pythonpath = ["engine-python", "."]
addopts = "-q --tb=short"
markers = [
    "slow: regenerates the golden corpus from scratch (~1 min); deselect with -m 'not slow'",
]

[tool.ruff]
line-length = 100
extend-exclude = ["node_modules", ".venv"]
